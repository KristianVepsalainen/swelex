% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/swe_get_text.R
\name{swe_get_metadata}
\alias{swe_get_metadata}
\title{Get metadata for a Swedish statute, without the full text}
\usage{
swe_get_metadata(sfs_nr)
}
\arguments{
\item{sfs_nr}{Character. SFS number in the format "1974:152".}
}
\value{
A one-row tibble with columns: \code{sfs_nr}, \code{titel}, \code{departement},
\code{utfardad}, \code{andrad_tom}, \code{upphavd}, \code{upphavd_av}, \code{is_repealed}.
}
\description{
A thin wrapper around \code{\link[=swe_get_doc]{swe_get_doc()}} that drops the \code{text} column.
Note: this still performs the same API call as \code{swe_get_doc()} — the
underlying endpoint does not support a text-free response — so it is a
convenience wrapper, not a lighter-weight one. See ROADMAP.md for a
possible future optimisation via the \code{dokumentlista} endpoint.
}
