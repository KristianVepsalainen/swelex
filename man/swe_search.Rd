% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/swe_search.R
\name{swe_search}
\alias{swe_search}
\title{Search Swedish statutes (SFS)}
\usage{
swe_search(
  query = NULL,
  from_date = NULL,
  to_date = NULL,
  org = NULL,
  rm = NULL,
  max_results = 50
)
}
\arguments{
\item{query}{Character. Free-text search term, matched against title and
body text. Optional.}

\item{from_date, to_date}{Character or Date, format "YYYY-MM-DD".
Restrict to statutes issued within this range. Optional.}

\item{org}{Character. Filter by issuing department/authority, e.g.
"Justitiedepartementet". Optional.}

\item{rm}{Character. Filter by Riksdag session year, e.g. "2022".
Optional.}

\item{max_results}{Integer. Maximum number of results to return.
Default 50.}
}
\value{
A tibble with columns: sfs_nr, titel, departement, datum,
dok_id, summary, score. Zero rows if nothing matched. Sorted by
relevance (descending) when \code{query} is supplied.
}
\description{
Searches Riksdagen's open data for statutes matching free text, a date
range, an issuing department, or a session year, sorted by relevance.
Unlike \code{swe_get_doc()}, an empty result set is not an error — it simply
means no statutes matched.
}
